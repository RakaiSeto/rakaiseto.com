version: "3.9"

services:
  rakaiseto:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - rakaiseto-com-network
    image: rakaiseto-com:latest
    container_name: rakaiseto-com
    restart: always
    ports:
      - "127.0.0.1:21001:80"

networks:
  rakaiseto-com-network:
